import processLanguage from '../src/client/js/formHandler'

describe("Testing the UI update functionality", () => {
    test("Testing the processLanguage() function", () => {
        expect(processLanguage).toBeDefined();
    }
    )})